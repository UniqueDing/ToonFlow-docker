services:
  toonflow:
    image: ghcr.io/uniqueding/toonflow-app:latest
    ports:
      - "${TOONFLOW_PORT:-10588}:10588"
    environment:
      NODE_ENV: prod
      PORT: 10588
      OSSURL: ${OSSURL:-http://127.0.0.1:10588/}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
